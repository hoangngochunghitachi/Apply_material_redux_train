const styles = (theme) => ({
    taskForm: {
        // margin: theme.spacing(1),
        // width: '100ch',
    },
    textField: {
        with: '200ch'
    },
    formControl: {
        // margin: theme.spacing(1),
        // minWidth: 120,
    }
})

export default styles;